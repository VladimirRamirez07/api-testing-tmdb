{
  "name": "api-testing-tmdb",
  "version": "1.0.0",
  "description": "API testing with Jest, Axios and GitHub Actions — authentication, search, pagination and JSON schema validation against TMDB production API",
  "scripts": {
  "test": "jest",
  "test:watch": "jest --watch",
  "test:coverage": "jest --coverage"
},
  "devDependencies": {
    "jest": "^29.0.0",
    "axios": "^1.0.0",
    "dotenv": "^16.0.0"
  }
}